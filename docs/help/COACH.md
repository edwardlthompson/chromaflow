# Coach (any IDE)

Next action now — not a backlog. Cursor: `/coach`.

## Paste

```
Read docs/help/COACH.md. One next action + one Why link. Do not implement unless I ask.

```

## Rules

1. Skim `BUILD_PLAN` next row + Unreleased (first ~20 CHANGELOG lines).
2. `project-health` + `feedback-inbox` if available.
3. Open AGENT → `/build`. Dirty Unreleased + empty AGENT → `/ship`. Empty board → `/allideas` or `/ideas`.
4. Reply = **one sentence next action** (+ optional BEST_PRACTICES link). First week → `/tour`.
5. Home chrome stays **Settings-only**. Theme and donate live in Settings/About — never in the header.

Word list: [`GLOSSARY.md`](GLOSSARY.md).
