[package]
name = "chromaflow-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "chromaflow inventory CLI (unprivileged, read-only)"

[[bin]]
name = "chromaflow"
path = "src/main.rs"

[dependencies]
chromaflow-core = { path = "../chromaflow-core" }
serde_json.workspace = true
