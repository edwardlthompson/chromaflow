# Update UX/UI construction guidelines

Patch [`docs/ux-ui-guidelines.md`](../../docs/ux-ui-guidelines.md) (construction law), not a review-only file. Keep `.cursor/rules/ux-ui.mdc` in sync for the build checklist. Cross-link `docs/DESIGN_GUIDE.md`; do not duplicate hex/token rules.

Include a resolved `### Critique`. Then `python3 scripts/agent-run.py validate-bootstrap --quick`.

Do not run a product `/ux-review` dump unless the user asked.

Begin now.
