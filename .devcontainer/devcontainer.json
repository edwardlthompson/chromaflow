{
  "name": "agent-project-bootstrap",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "features": {
    "ghcr.io/devcontainers/features/python:1": {
      "version": "3.12"
    },
    "ghcr.io/devcontainers/features/common-utils:2": {
      "installZsh": true,
      "installOhMyZsh": false
    },
    "ghcr.io/devcontainers/features/github-cli:1": {}
  },
  "containerEnv": {
    "ANDROID_HOME": "/opt/android-sdk",
    "ANDROID_SDK_ROOT": "/opt/android-sdk"
  },
  "postCreateCommand": "pip install pre-commit && cd examples/python && curl -LsSf https://astral.sh/uv/install.sh | sh && uv sync --all-extras && cd ../web && npm install && cd ../.. && bash scripts/install-android-cmdline-tools.sh || true",
  "postStartCommand": "bash scripts/check-file-encoding.sh || true; bash scripts/verify.sh || echo 'WARN: verify failed — run bash scripts/verify.sh'; bash scripts/check-template-updates.sh || true; echo 'Tip: pick Cursor mode via docs/CURSOR_MODES.md before editing'; echo 'Tip: after push to main, run scripts/check-github-ci.sh --wait 300'",
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "ms-python.python",
        "charliermarsh.ruff",
        "bradlc.vscode-tailwindcss"
      ],
      "settings": {
        "editor.formatOnSave": true,
        "editor.defaultFormatter": "esbenp.prettier-vscode"
      }
    }
  },
  "remoteUser": "node"
}
